<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cc.mapper.CommentMapper">
<!--    根据a_id查找评论-->
    <select id="findById" resultType="comment" parameterType="int">
        select * from t_comment where a_id=#{a_id}
    </select>
</mapper>